= simple_form_for @category do |f|
  = f.input :context, label: t('.context')
  = f.input :label
  = f.input :position
  = f.input :kind, collection: EveryMatrix::Category.kinds
  = f.submit 'Save', class: 'btn btn-success'
