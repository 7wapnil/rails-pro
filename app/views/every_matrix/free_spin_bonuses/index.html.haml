%h1.display-4
  = t('internal.entities.free_spin_bonuses')
  = link_to t('internal.create'),
            new_every_matrix_free_spin_bonus_path,
            class: 'btn btn-outline-primary float-right'

= render 'table',
          filter: @filter,
          excluded_columns: []

= paginate @filter.free_spin_bonuses
