= label_tag :play_item_ids, t('attributes.games')
= select_tag :play_item_id,
             options_from_collection_for_select(@play_items, :id, :name),
             class: 'form-control select required',
             size: 12
