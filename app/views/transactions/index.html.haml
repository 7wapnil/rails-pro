%h1.display-4= t('internal.entities.transactions')
= render 'search',
         filter: @filter,
         search_path: transactions_path
= render 'table', filter: @filter
= paginate @filter.transactions
