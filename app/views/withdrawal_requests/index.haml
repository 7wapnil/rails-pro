%h1.display-4= t('entities.withdrawal_requests')
= render 'filter',
         filter: @filter,
         search_path: customers_path
= render 'table', filter: @filter
= paginate @filter.withdrawal_requests
