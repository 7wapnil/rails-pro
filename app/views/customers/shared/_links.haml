- if controller_name != 'sessions'
  = link_to t(:sign_in), new_session_path(resource_name), class: 'btn btn-outline-secondary'

- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to t(:sign_up), new_registration_path(resource_name), class: 'btn btn-outline-secondary'
