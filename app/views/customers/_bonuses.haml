= card header: t('internal.entities.bonuses'), class: 'bonuses' do
  - if history.any?
    %table.table
      %thead
        %th= t('internal.attributes.code')
        %th= t('internal.attributes.activated_at')
        %th= t('internal.attributes.status')
      %tbody
        - history.each do |customer_bonus|
          %tr
            %td= link_to customer_bonus.code, customer_bonus
            %td= l(customer_bonus.created_at)
            %td= status_badge(customer_bonus.status)
  - else
    .text-center
      %em= t('internal.no_records')
