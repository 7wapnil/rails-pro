= card class: 'mb-4', header: t('entities.deposit_limit') do
  = render 'customers/deposit_limit_form',
            limit: customer.deposit_limit,
            currencies: currencies_with_primary_for(customer),
            customer: customer
