= card header: t('documents.history_title',
                 document_type: t("attributes.#{@document_type}")),
       class: 'customer-documents' do
  %table.table.table-striped
    %tbody
      - @files.each do |file|
        %tr
          %td= file.filename
          %td= file.created_at
          %td= link_to t(:view),
                      rails_blob_path(file),
                      class: 'btn btn-outline-dark text-right',
                      target: :blank
