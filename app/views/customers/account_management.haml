- content_for :page_content do
  .mt-4= render 'customers/balances'
  .mt-4= render 'customers/entry_request_form'
  .mt-4.mb-4= render 'customers/balance_interactions', entries: @entries
  .mt-4.mb-4= render 'customers/entry_requests', entry_requests: @entry_requests

- content_for :js do
  = javascript_pack_tag 'customers/navigation_links'

= render 'customers/base_template', customer: @customer, note: @note,
  customer_notes_widget: @customer_notes_widget
